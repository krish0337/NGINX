apiVersion: v1
kind: Pod
metadata:
  name: Nginx.example.com
  labels:
    app: Nginx.example.com
spec:
  containers:
  - name: k8s-demo2
    image: docker pull nginx
    ports:
    - name: Nginx-port
      containerPort: 80
  
